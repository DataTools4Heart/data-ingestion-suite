{
  "id": "icrc-medication-mapping",
  "url": "https://datatools4heart.eu/fhir/mappings/icrc/medication-mapping",
  "name": "medication-mapping",
  "title": "Mapping of icrc medication data to DT4H MedicationAdministration FHIR profile",
  "source": [{
    "alias": "source",
    "url": "https://datatools4heart.eu/icrc/Medication"
  }],
  "mapping": [
    {
      "precondition": {
        "name": "encounterExists",
        "language": "text/fhirpath",
        "expression": "encounter.exists()"
      },
      "expression": {
        "name": "result",
        "language": "application/fhir-template+json",
        "value": {
          "resourceType": "MedicationAdministration",
          "id": "{{mpp:getHashedId('MedicationAdministration', subject.toString() + encounter.toString() + code_atc.toString())}}",
          "meta": {
            "profile": [
              "https://datatools4heart.eu/fhir/StructureDefinition/DT4H-MedicationAdministration"
            ],
            "source": "{{%sourceSystem.sourceUri}}"
          },
          "status": "{{iif(date_to.exists(), 'completed', 'in-progress')}}",
          "medicationCodeableConcept": {
            "coding": [
              {
                "system": "http://www.whocc.no/atc",
                "code": "{{code_atc}}",
                "display": "{{? name_en}}"
              }
            ]
          },
          "subject": "{{mpp:createFhirReferenceWithHashedId('Patient', subject.toString())}}",
          "effectivePeriod": {
            "start": "{{date_from}}",
            "end": "{{? date_to}}"
          },
          "context": "{{mpp:createFhirReferenceWithHashedId('Encounter', encounter.toString())}}",
          "dosage": {
            "dose": {
              "value": "{{dosage}}"
            }
          }
        }
      }
    }
  ]
}
